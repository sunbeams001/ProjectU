# ProjectU - 项目架构参考文档

> 📅 **最后更新**: 2025-12-23  
> 🎯 **当前状态**: 核心功能已完成，完整 Pixiv API 集成，下载系统已实现

---

## 一、项目概述

ProjectU 是一个基于 Kotlin Multiplatform 的跨平台 Pixiv 客户端，支持 Android 和 Desktop 平台。项目采用 Clean Architecture + MVI 架构模式，使用最新的 Compose Multiplatform 技术栈。

### 核心特性

- 🎨 **跨平台支持**: Android (API 24+) 和 Desktop (Windows/Mac/Linux)
- 🌍 **完整多语言**: 简体中文、繁体中文、英文、日文、韩文
- 🎭 **Material Design 3**: 深色/浅色主题，Pixiv 风格配色
- 🏗️ **Clean Architecture**: Domain + Data + Presentation 分层
- 🔄 **MVI 架构**: 单向数据流，可预测的状态管理
- 🌐 **Pixiv Web API**: 完整集成官方 Web API
- 🎬 **Ugoira 支持**: Pixiv 动图下载和播放
- 📱 **响应式设计**: 自适应手机/平板/桌面布局

---

## 二、项目模块划分

### 1. composeApp 模块

**职责**: 表现层（Presentation Layer），包含所有 UI 和视图逻辑

```
composeApp/
├── src/
│   ├── commonMain/                          # 共享 UI 代码
│   │   ├── kotlin/com/projectu/
│   │   │   ├── App.kt                      # 应用入口，配置 Koin 和导航
│   │   │   ├── ui/
│   │   │   │   ├── screens/                # 各个页面
│   │   │   │   │   ├── home/              # 主页（含底部导航）
│   │   │   │   │   ├── settings/          # 设置页面
│   │   │   │   │   ├── artwork/           # 作品详情页
│   │   │   │   │   ├── user/              # 用户主页
│   │   │   │   │   ├── discovery/         # 发现页
│   │   │   │   │   ├── ranking/           # 排行榜
│   │   │   │   │   ├── search/            # 搜索页
│   │   │   │   │   ├── download/          # 下载管理
│   │   │   │   │   ├── novel/             # 小说相关
│   │   │   │   │   ├── mangaseries/       # 漫画系列
│   │   │   │   │   ├── comment/           # 评论
│   │   │   │   │   ├── login/             # 登录
│   │   │   │   │   ├── followlatest/      # 关注用户动态
│   │   │   │   │   ├── userrelations/     # 用户关系
│   │   │   │   │   └── apitest/           # API测试工具
│   │   │   │   ├── components/             # 可复用组件
│   │   │   │   │   ├── UgoiraPlayer.kt    # Ugoira 动图播放器
│   │   │   │   │   └── AdaptiveLayout.kt  # 响应式布局容器
│   │   │   │   ├── theme/                  # Material 3 主题
│   │   │   │   │   ├── Theme.kt           # 主题配置（浅/深色）
│   │   │   │   │   └── Type.kt            # 字体排版
│   │   │   │   ├── localization/          # 多语言管理
│   │   │   │   │   └── LocaleManager.kt   # 语言切换和持久化
│   │   │   │   └── util/                   # UI 工具类
│   │   │   │       └── WindowSizeClass.kt # 窗口尺寸分类
│   │   │   ├── di/
│   │   │   │   └── AppModule.kt           # UI 层 DI 配置
│   │   │   └── util/
│   │   │       └── WindowSizeCalculator.kt
│   │   └── composeResources/               # Compose 多语言资源
│   │       ├── values/                     # 英文（默认）
│   │       ├── values-zh-rCN/             # 简体中文
│   │       ├── values-zh-rTW/             # 繁体中文
│   │       ├── values-ja/                 # 日文
│   │       └── values-ko/                 # 韩文
│   ├── androidMain/                        # Android 平台
│   │   └── kotlin/com/projectu/
│   │       ├── MainActivity.kt            # Android 入口
│   │       ├── PixivApplication.kt        # Application 类
│   │       ├── di/
│   │       │   └── PlatformModule.android.kt  # Android DI
│   │       └── util/
│   │           └── WindowSizeCalculator.kt
│   └── desktopMain/                        # Desktop 平台
│       └── kotlin/com/projectu/
│           ├── main.kt                    # Desktop 入口
│           ├── di/
│           │   └── PlatformModule.desktop.kt  # Desktop DI
│           └── util/
│               └── WindowSizeCalculator.kt
└── build.gradle.kts
```

### 2. shared 模块

**职责**: 业务逻辑层，包含领域模型、数据访问、业务用例

```
shared/
├── src/
│   ├── commonMain/
│   │   └── kotlin/com/projectu/shared/
│   │       ├── domain/                     # 领域层（业务核心）
│   │       │   ├── model/                  # 领域模型
│   │       │   │   └── Artwork.kt         # 作品模型
│   │       │   ├── repository/             # Repository 接口定义
│   │       │   │   ├── ArtworkRepository.kt
│   │       │   │   ├── UserRepository.kt
│   │       │   │   └── SettingsRepository.kt
│   │       │   └── usecase/                # 业务用例
│   │       │       ├── GetUgoiraUseCase.kt
│   │       │       ├── BookmarkArtworkUseCase.kt
│   │       │       ├── UnbookmarkArtworkUseCase.kt
│   │       │       ├── SyncArtworkStatesUseCase.kt
│   │       │       ├── BookmarkNovelUseCase.kt
│   │       │       ├── UnbookmarkNovelUseCase.kt
│   │       │       ├── SyncNovelStatesUseCase.kt
│   │       │       ├── FollowUserUseCase.kt
│   │       │       ├── UnfollowUserUseCase.kt
│   │       │       ├── SyncUserStatesUseCase.kt
│   │       │       └── SyncUserFollowDetailsUseCase.kt
│   │       ├── data/                       # 数据层
│   │       │   ├── repository/             # Repository 实现
│   │       │   │   ├── ArtworkRepositoryImpl.kt
│   │       │   │   ├── UserRepositoryImpl.kt
│   │       │   │   └── SettingsRepositoryImpl.kt
│   │       │   ├── remote/                 # 网络数据源
│   │       │   │   ├── api/               # Pixiv API 接口
│   │       │   │   │   ├── PixivApi.kt    # API 统一门面
│   │       │   │   │   ├── PixivApiClient.kt  # HTTP 客户端
│   │       │   │   │   ├── IllustApi.kt   # 插画 API
│   │       │   │   │   ├── IllustSeriesApi.kt # 漫画系列 API
│   │       │   │   │   ├── UserApi.kt     # 用户 API
│   │       │   │   │   ├── BookmarkApi.kt # 收藏 API
│   │       │   │   │   ├── RankingApi.kt  # 排行榜 API
│   │       │   │   │   ├── CommentApi.kt  # 评论 API
│   │       │   │   │   ├── NovelApi.kt    # 小说 API
│   │       │   │   │   ├── NovelSeriesApi.kt # 小说系列 API
│   │       │   │   │   ├── TagApi.kt      # 标签 API
│   │       │   │   │   ├── MarkerApi.kt   # 书签 API（稍后再读）
│   │       │   │   │   ├── FollowApi.kt   # 关注 API（已关注用户的作品、追更）
│   │       │   │   │   └── SearchApi.kt   # 搜索 API
│   │       │   │   ├── dto/               # 数据传输对象
│   │       │   │   │   ├── pixiv/         # Pixiv API DTO
│   │       │   │   │   │   ├── PixivResponse.kt
│   │       │   │   │   │   ├── IllustDto.kt
│   │       │   │   │   │   ├── UserDto.kt
│   │       │   │   │   │   └── RankingDto.kt
│   │       │   │   │   └── ArtworkDto.kt
│   │       │   │   └── mapper/            # DTO → Domain 映射
│   │       │   │       ├── PixivArtworkMapper.kt
│   │       │   │       └── ArtworkMapper.kt
│   │       │   ├── local/                  # 本地数据源
│   │       │   │   ├── database/          # Room 数据库
│   │       │   │   │   ├── AppDatabase.kt
│   │       │   │   │   ├── DatabaseBuilder.kt
│   │       │   │   │   ├── Database.android.kt  # Android 实现
│   │       │   │   │   └── Database.desktop.kt  # Desktop 实现
│   │       │   │   ├── dao/               # Room DAO
│   │       │   │   │   ├── ArtworkDao.kt
│   │       │   │   │   └── SettingsDao.kt
│   │       │   │   ├── entity/            # 数据库实体
│   │       │   │   │   ├── ArtworkEntity.kt
│   │       │   │   │   ├── SettingsEntity.kt
│   │       │   │   │   ├── DownloadTaskEntity.kt
│   │       │   │   │   └── DownloadRuleEntity.kt
│   │       │   │   ├── AppSettings.kt     # 应用设置模型
│   │       │   │   ├── PixivConfig.kt     # Pixiv 配置模型
│   │       │   │   ├── SettingsStore.kt   # 设置存储（Room）
│   │       │   │   └── PixivConfigStore.kt # Pixiv 配置存储（DataStore）
│   │       │   └── cache/                  # 缓存管理
│   │       │       ├── UgoiraCache.kt     # Ugoira 缓存（共享）
│   │       │       ├── StateCacheManager.kt   # 全局状态缓存管理器
│   │       │       ├── SettingsCache.kt   # 设置缓存
│   │       │       ├── ArtworkCacheManager.kt # 作品缓存管理器
│   │       │       ├── UgoiraCache.android.kt  # Android ZIP 解压
│   │       │       └── UgoiraCache.desktop.kt  # Desktop ZIP 解压
│   │       ├── util/                       # 工具类
│   │       │   └── NetworkClient.kt       # Ktor 客户端配置
│   │       ├── manager/                    # 管理器
│   │       │   └── DownloadManager.kt     # 下载管理器
│   │       ├── di/
│   │       │   └── SharedModule.kt        # Shared 层 DI 配置
│   │       └── examples/
│   │           └── PixivApiUsageExample.kt # API 使用示例
│   ├── androidMain/
│   │   └── kotlin/com/projectu/shared/
│   │       ├── data/
│   │       │   ├── cache/
│   │       │   │   └── UgoiraCache.android.kt
│   │       │   └── local/database/
│   │       │       └── Database.android.kt
│   └── desktopMain/
│       └── kotlin/com/projectu/shared/
│           ├── data/
│           │   ├── cache/
│           │   │   └── UgoiraCache.desktop.kt
│           │   └── local/database/
│           │       └── Database.desktop.kt
└── build.gradle.kts
```

---

## 三、架构设计

### 3.1 Clean Architecture 分层

项目严格遵循 Clean Architecture 原则，实现关注点分离和依赖倒置：

```
┌─────────────────────────────────────────────────────────────┐
│                   Presentation Layer                        │
│                  (composeApp 模块)                           │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  UI (Jetpack Compose)                                 │  │
│  │  • Screens - 页面实现（HomeScreen, SettingsScreen）    │  │
│  │  • Components - 可复用 UI 组件（UgoiraPlayer）        │  │
│  │  • ViewModels - 状态管理（SettingsViewModel）         │  │
│  │  • Theme - Material 3 主题配置                        │  │
│  │  • Localization - 多语言支持（LocaleManager）         │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓ 依赖
┌─────────────────────────────────────────────────────────────┐
│                     Domain Layer                            │
│               (shared/domain/)                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Business Logic (纯 Kotlin，无外部依赖)               │  │
│  │  • Models - 领域模型（Artwork）                       │  │
│  │  • Repository Interfaces - 数据访问接口               │  │
│  │  • UseCases - 业务用例（GetUgoiraUseCase）           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓ 实现
┌─────────────────────────────────────────────────────────────┐
│                      Data Layer                             │
│                 (shared/data/)                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Data Sources & Repository Implementations            │  │
│  │  • Repository Impl - 实现 Domain 接口                 │  │
│  │  • Remote (Ktor) - Pixiv Web API 客户端              │  │
│  │  • Local (Room) - SQLite 数据库持久化                │  │
│  │  • Cache (Okio) - 文件缓存和 ZIP 处理                │  │
│  │  • DataStore - 键值对配置存储                        │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**依赖规则**:
- ✅ Presentation 层可依赖 Domain 层（通过接口）
- ✅ Data 层实现 Domain 层定义的接口
- ❌ Domain 层不依赖任何外部框架
- ❌ Data 层不能直接被 Presentation 调用

### 3.2 MVI (Model-View-Intent) 架构

每个 Screen 都遵循 MVI 单向数据流模式：

```
┌──────────────────────────────────────────────────────────┐
│                         View                              │
│          (Composable Functions)                          │
│  • 渲染 State                                            │
│  • 发出 Intent                                           │
└──────────────────────────────────────────────────────────┘
            │                                    ↑
            │ User Intent                        │ State Update
            ↓                                    │
┌──────────────────────────────────────────────────────────┐
│                      ViewModel                            │
│  • 接收 Intent                                           │
│  • 调用 UseCase                                          │
│  • 更新 State (StateFlow)                                │
└──────────────────────────────────────────────────────────┘
            │                                    ↑
            │ UseCase Call                       │ Result
            ↓                                    │
┌──────────────────────────────────────────────────────────┐
│                      UseCase                              │
│  • 业务逻辑处理                                          │
│  • 调用 Repository                                       │
└──────────────────────────────────────────────────────────┘
            │                                    ↑
            │ Data Request                       │ Data Response
            ↓                                    │
┌──────────────────────────────────────────────────────────┐
│                     Repository                            │
│  • 协调多个数据源                                        │
│  • 缓存策略                                              │
│  • 数据映射 (DTO → Domain Model)                        │
└──────────────────────────────────────────────────────────┘
            │                                    │
      ┌─────┴───────┬──────────────────┬────────┴─────┐
      ↓             ↓                  ↓              ↓
┌─────────┐  ┌──────────┐    ┌─────────────┐  ┌──────────┐
│ Remote  │  │  Local   │    │   Cache     │  │DataStore │
│  API    │  │   DB     │    │   (File)    │  │  (KV)    │
│ (Ktor)  │  │ (Room)   │    │  (Okio)     │  │          │
└─────────┘  └──────────┘    └─────────────┘  └──────────┘
```

**实际示例 (SettingsScreen)**:

```kotlin
// State - 屏幕状态
data class SettingsScreenState(
    val appLanguage: AppLanguage,
    val pixivLanguage: PixivLanguage,
    val themeMode: ThemeMode,
    val isLoading: Boolean = false
)

// Intent - 用户操作
sealed interface SettingsIntent {
    data class ChangeAppLanguage(val language: AppLanguage) : SettingsIntent
    data class ChangePixivLanguage(val language: PixivLanguage) : SettingsIntent
    data class ChangeTheme(val mode: ThemeMode) : SettingsIntent
}

// ViewModel - 状态管理
class SettingsViewModel(
    private val settingsRepository: SettingsRepository
) : ViewModel() {
    private val _state = MutableStateFlow(SettingsScreenState(...))
    val state: StateFlow<SettingsScreenState> = _state.asStateFlow()
    
    fun handleIntent(intent: SettingsIntent) {
        when (intent) {
            is SettingsIntent.ChangeAppLanguage -> {
                viewModelScope.launch {
                    settingsRepository.updateAppLanguage(intent.language)
                }
            }
            // ... 其他 Intent 处理
        }
    }
}
```

### 3.3 依赖注入 (Koin)

使用 Koin 实现跨平台依赖注入，支持平台特定实现：

**通用模块** (`composeApp/di/AppModule.kt`):
```kotlin
val commonModule = module {
    single { LocaleManager(get()) }
}
```

**平台特定模块** (`PlatformModule.android.kt` / `PlatformModule.desktop.kt`):
```kotlin
// Android
actual val networkModule = module {
    single { NetworkClient.create(CIO.create()) }
}

actual val databaseModule = module {
    single { getRoomDatabase(getDatabaseBuilder()) }
    single<SettingsDao> { get<AppDatabase>().settingsDao() }
}

// Desktop 也有对应实现，使用不同的数据库路径
```

**模块组织结构**:
```
initKoin()
  ├── commonModule        (跨平台共享)
  ├── networkModule       (平台特定: CIO Engine)
  ├── databaseModule      (平台特定: Room DB 路径)
  ├── repositoryModule    (共享: Repository 实现)
  ├── useCaseModule       (共享: UseCase)
  └── viewModelModule     (平台特定: Android 用 viewModel {})
```

---

## 四、模块依赖关系

### 4.1 Gradle 模块依赖

```
┌─────────────────┐
│   composeApp    │  (Android App / Desktop App)
│                 │
│  • UI 层        │
│  • ViewModels   │
│  • Navigation   │
│  • Themes       │
└────────┬────────┘
         │ implementation(projects.shared)
         ↓
┌─────────────────┐
│     shared      │  (Kotlin Multiplatform Library)
│                 │
│  • Domain 层    │
│  • Data 层      │
│  • Repositories │
│  • UseCases     │
└─────────────────┘
```

**原则**:
- composeApp 依赖 shared（单向依赖）
- shared 是独立的 KMP 库，可复用到其他项目
- 不存在循环依赖

### 4.2 架构层级依赖

```
Presentation Layer (composeApp)
         │
         │ 依赖 Repository 接口
         ↓
Domain Layer (shared/domain)        ← 核心业务逻辑，无外部依赖
         ↑
         │ 实现接口
         │
Data Layer (shared/data)
         │
         ├─→ Remote (Ktor)
         ├─→ Local (Room)
         ├─→ Cache (Okio)
         └─→ DataStore
```

**依赖倒置原则 (DIP)**:
- Domain 层定义 `Repository` 接口
- Data 层实现 `RepositoryImpl`
- Presentation 层通过 Koin 注入接口，不直接依赖实现类

---

## 五、关键技术实现

### 5.1 网络层 - Pixiv Web API 集成

#### API 架构

采用分层 API 设计，提供统一的访问入口：

```kotlin
// 1. PixivApiClient - HTTP 客户端封装
class PixivApiClient(
    httpClient: HttpClient,
    phpSessionId: String,      // 从浏览器 Cookie 获取
    token: String? = null,     // CSRF Token（自动获取）
    host: String = "https://www.pixiv.net",
    lang: String = "zh"
) {
    // 自动处理：
    // • Cookie 认证 (PHPSESSID)
    // • CSRF Token 刷新
    // • 错误重试
    // • 请求日志
}

// 2. API 模块化 - 按功能划分
class PixivApi(client: PixivApiClient) {
    val illustApi: IllustApi        // 插画相关
    val userApi: UserApi            // 用户相关
    val bookmarkApi: BookmarkApi    // 收藏相关
    val rankingApi: RankingApi      // 排行榜相关
}
```

#### 支持的 API 功能

| API 模块 | 功能 | 状态 |
|---------|------|------|
| **IllustApi** | 作品详情、搜索、推荐、发现、Ugoira 元数据 | ✅ |
| **IllustSeriesApi** | 漫画系列管理、追更 | ✅ |
| **UserApi** | 用户信息、关注列表、作品列表、收藏列表 | ✅ |
| **BookmarkApi** | 添加/删除收藏、批量操作、收藏标签 | ✅ |
| **RankingApi** | 日榜、周榜、月榜、新人榜等 | ✅ |
| **CommentApi** | 评论管理 | ✅ |
| **NovelApi** | 小说相关功能 | ✅ |
| **NovelSeriesApi** | 小说系列管理 | ✅ |
| **TagApi** | 标签相关功能 | ✅ |
| **MarkerApi** | 书签管理（稍后再读） | ✅ |
| **FollowApi** | 关注用户的作品、追更 | ✅ |
| **SearchApi** | 搜索功能 | ✅ |

**详细文档**: 查看 `docs/guides/API测试工具使用指南.md`

#### 网络客户端配置

**位置**: `shared/util/NetworkClient.kt`

```kotlin
fun create(engine: HttpClientEngine) = HttpClient(engine) {
    install(ContentNegotiation) {
        json(Json { 
            ignoreUnknownKeys = true    // 兼容 API 变更
            isLenient = true 
        })
    }
    
    install(Logging) {
        level = LogLevel.INFO           // 调试时可改为 ALL
    }
    
    install(HttpTimeout) {
        requestTimeoutMillis = 60000    // 60 秒超时
    }
}
```

### 5.2 数据持久化

#### Room 数据库 (跨平台)

**数据库定义** (`AppDatabase.kt`):
```kotlin
@Database(
    entities = [
        ArtworkEntity::class,
        SettingsEntity::class,
        DownloadTaskEntity::class,
        DownloadRuleEntity::class
    ],
    version = 1
)
@ConstructedBy(AppDatabaseConstructor::class)
abstract class AppDatabase : RoomDatabase() {
    abstract fun artworkDao(): ArtworkDao
    abstract fun settingsDao(): SettingsDao
    abstract fun downloadDao(): DownloadDao
    abstract fun downloadRulesDao(): DownloadRulesDao
}
```

**平台特定实现**:

**Android** (`Database.android.kt`):
```kotlin
fun getDatabaseBuilder(): RoomDatabase.Builder<AppDatabase> {
    val context = ContextHolder.getContext()
    val dbFile = context.getDatabasePath("projectu.db")
    return Room.databaseBuilder<AppDatabase>(
        context = context,
        name = dbFile.absolutePath
    )
}
```

**Desktop** (`Database.desktop.kt`):
```kotlin
fun getDatabaseBuilder(): RoomDatabase.Builder<AppDatabase> {
    val dbFile = File(System.getProperty("user.home"), ".projectu/projectu.db")
    dbFile.parentFile?.mkdirs()
    return Room.databaseBuilder<AppDatabase>(
        name = dbFile.absolutePath
    )
}
```

**实体示例**:
```kotlin
@Entity(tableName = "settings")
data class SettingsEntity(
    @PrimaryKey val id: Int = 1,
    val appLanguage: String,        // "SIMPLIFIED_CHINESE"
    val pixivLanguage: String,      // "zh"
    val themeMode: String           // "SYSTEM"
)
```

#### DataStore (键值对存储)

用于存储 Pixiv 配置信息：

**位置**: `shared/data/local/PixivConfigStore.kt`

```kotlin
class PixivConfigStore(context: PlatformContext) {
    private val dataStore = createDataStore(
        producePath = { 
            // Android: context.filesDir
            // Desktop: ~/.projectu/pixiv_config.preferences_pb
        }
    )
    
    val config: Flow<PixivConfig> = dataStore.data.map { prefs ->
        PixivConfig(
            phpSessionId = prefs[PHP_SESSION_ID] ?: "",
            csrfToken = prefs[CSRF_TOKEN],
            language = prefs[LANGUAGE] ?: "zh"
        )
    }
    
    suspend fun updatePhpSessionId(value: String) {
        dataStore.edit { it[PHP_SESSION_ID] = value }
    }
}
```

### 5.3 Ugoira 动图处理

Pixiv 的 Ugoira (うごイラ) 是一种特殊的动图格式，由多张静态图片按时序播放。

#### 处理流程

```
1. 获取元数据
   ↓
   IllustApi.getUgoiraMeta(illustId)
   返回: { zipUrl, frames: [{ delay: 100 }, ...] }

2. 下载 ZIP 文件
   ↓
   UgoiraCache.downloadAndExtract(zipUrl, illustId)
   保存到: Android - cache/ugoira/
          Desktop - ~/.projectu/cache/ugoira/

3. 解压图片帧
   ↓
   [Platform-specific]
   Android: java.util.zip.ZipInputStream
   Desktop: java.util.zip.ZipFile
   
4. 播放动画
   ↓
   UgoiraPlayer Composable
   使用 LaunchedEffect + delay 循环播放
```

#### 关键实现

**缓存管理** (`UgoiraCache.kt`):
```kotlin
class UgoiraCache(private val cacheDir: File) {
    suspend fun downloadAndExtract(
        zipUrl: String, 
        illustId: String
    ): List<File> {
        val zipFile = downloadZip(zipUrl, illustId)
        return extractZipPlatform(zipFile, illustId)  // expect 函数
    }
    
    fun getFrames(illustId: String): List<File> {
        return File(cacheDir, illustId).listFiles()
            ?.sortedBy { it.name } 
            ?: emptyList()
    }
}
```

**播放器组件** (`UgoiraPlayer.kt`):
```kotlin
@Composable
fun UgoiraPlayer(
    frames: List<File>,          // 图片帧文件
    delays: List<Int>,           // 每帧延迟（毫秒）
    modifier: Modifier = Modifier
) {
    var currentFrame by remember { mutableStateOf(0) }
    
    LaunchedEffect(frames, delays) {
        while (true) {
            delay(delays[currentFrame].toLong())
            currentFrame = (currentFrame + 1) % frames.size
        }
    }
    
    AsyncImage(
        model = frames[currentFrame],
        contentDescription = null,
        modifier = modifier
    )
}
```

### 5.4 多语言系统

#### Compose Resources (官方方案)

使用 Jetpack Compose 官方的多语言方案，替代了 Moko Resources。

**资源结构**:
```
composeApp/src/commonMain/composeResources/
├── values/                 # 英文（默认）
│   └── strings.xml
├── values-zh-rCN/         # 简体中文
│   └── strings.xml
├── values-zh-rTW/         # 繁体中文
│   └── strings.xml
├── values-ja/             # 日文
│   └── strings.xml
└── values-ko/             # 韩文
    └── strings.xml
```

**字符串定义** (`values/strings.xml`):
```xml
<resources>
    <string name="nav_home">Home</string>
    <string name="nav_discovery">Discovery</string>
    <string name="settings_title">Settings</string>
    <string name="settings_app_language">App Language</string>
</resources>
```

**在代码中使用**:
```kotlin
import org.jetbrains.compose.resources.stringResource
import projectu.composeapp.generated.resources.Res

@Composable
fun MyScreen() {
    Text(stringResource(Res.string.nav_home))
}
```

#### 语言管理器

**LocaleManager** 负责语言切换和持久化：

```kotlin
class LocaleManager(
    private val settingsRepository: SettingsRepository
) {
    private val _currentLanguage = MutableStateFlow(AppLanguage.SIMPLIFIED_CHINESE)
    val currentLanguage: StateFlow<AppLanguage> = _currentLanguage.asStateFlow()
    
    // 设置语言
    fun setLanguage(language: AppLanguage) {
        _currentLanguage.value = language
        
        // 1. 更新 Java Locale (影响 Compose Resources 选择)
        java.util.Locale.setDefault(language.toJavaLocale())
        
        // 2. 保存到数据库
        settingsRepository.updateAppLanguage(language)
    }
}
```

**应用语言与 Pixiv API 语言分离**:
```kotlin
data class AppSettings(
    val appLanguage: AppLanguage,      // UI 语言 (zh-CN, en, ja...)
    val pixivLanguage: PixivLanguage,  // API 语言 (zh, en, ja...)
    val themeMode: ThemeMode
)
```

### 5.5 响应式布局

#### 窗口尺寸分类

```kotlin
enum class WindowSizeClass {
    COMPACT,    // 手机竖屏 (< 600dp)
    MEDIUM,     // 手机横屏 / 小平板 (600dp - 840dp)
    EXPANDED    // 平板 / 桌面 (> 840dp)
}

@Composable
expect fun calculateWindowSizeClass(): WindowSizeClass
```

#### 自适应布局容器

```kotlin
@Composable
fun SimpleAdaptiveLayout(
    phoneContent: @Composable (WindowSize) -> Unit,
    tabletContent: @Composable (WindowSize) -> Unit
) {
    val windowSize = calculateWindowSizeClass()
    
    when (windowSize) {
        WindowSizeClass.COMPACT -> phoneContent(windowSize)
        WindowSizeClass.MEDIUM, 
        WindowSizeClass.EXPANDED -> tabletContent(windowSize)
    }
}
```

**HomeScreen 的响应式实现**:
- **手机**: 底部导航栏 (NavigationBar)
- **平板/桌面**: 侧边导航栏 (NavigationRail)

### 5.6 全局状态缓存系统

#### 设计目标

解决跨页面、跨组件的收藏/关注状态一致性问题，确保用户操作后所有相关界面立即同步更新。

#### 核心组件

**StateCacheManager** - 统一状态管理器：

```kotlin
class StateCacheManager(
    private val stateCacheRepository: StateCacheRepository
) {
    // 状态变更事件流
    private val _stateChangeEvents = MutableSharedFlow<StateCacheEvent>()
    val stateChangeEvents: SharedFlow<StateCacheEvent> = _stateChangeEvents.asSharedFlow()
    
    // 收藏作品
    suspend fun bookmarkArtwork(artworkId: String, isPrivate: Boolean, bookmarkId: String)
    
    // 取消收藏
    suspend fun unbookmarkArtwork(artworkId: String)
    
    // 应用状态到作品列表（自动同步缓存状态）
    suspend fun applyStatesToArtworks(artworks: List<Artwork>): List<Artwork>
}
```

**StateCacheRepository** - 状态存储：

```kotlin
interface StateCacheRepository {
    // 查询状态
    fun getStateCacheEntry(id: String, type: StateCacheType): Flow<StateCacheEntry?>
    
    // 批量查询
    suspend fun getArtworkStates(artworkIds: List<String>): Map<String, StateCacheEntry>
    
    // 更新状态
    suspend fun updateArtworkBookmarkStatus(artworkId: String, status: BookmarkStatus, bookmarkId: String?)
}
```

#### UseCase 封装

- **BookmarkArtworkUseCase** - 收藏作品，调用 API + 更新缓存
- **UnbookmarkArtworkUseCase** - 取消收藏
- **SyncArtworkStatesUseCase** - 同步作品列表状态
- **BookmarkNovelUseCase** / **UnbookmarkNovelUseCase** - 小说收藏
- **FollowUserUseCase** / **UnfollowUserUseCase** - 用户关注
- **SyncUserFollowDetailsUseCase** - 精确同步关注类型（公开/悄悄关注）

#### 使用示例

```kotlin
// ViewModel 中监听状态变更
init {
    viewModelScope.launch {
        stateCacheManager.stateChangeEvents.collect { event ->
            when (event) {
                is StateCacheEvent.ArtworkBookmarkChanged -> {
                    // 更新列表中的作品状态
                    updateArtworkInList(event.artworkId, event.status)
                }
            }
        }
    }
}

// 加载数据时自动同步状态
val artworks = artworkRepository.getRecommendedArtworks()
val syncedArtworks = stateCacheManager.applyStatesToArtworks(artworks)
```

**详细文档**: [docs/guides/全局状态缓存使用指南.md](../guides/全局状态缓存使用指南.md)

### 5.7 下载系统

#### 系统架构

```
DownloadManager（下载管理器）
    ├─→ DownloadTask（任务模型）
    ├─→ DownloadRulesManager（路径规则引擎）
    ├─→ FileNamingEngine（文件命名引擎）
    └─→ PlatformFileWriter（平台文件写入）
```

#### 核心功能

**1. 多格式支持**
- 插画/漫画：单页/多页，原图质量
- Ugoira：ZIP/GIF/MP4 三种格式
- 小说：TXT/EPUB，内嵌图片

**2. 路径规则系统**

基于优先级的规则匹配引擎：

```kotlin
@Entity(tableName = "download_rules")
data class DownloadRuleEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String,                    // 规则名称
    val priority: Int,                   // 优先级（数字越小越优先）
    val resourceType: ResourceType,      // 资源类型过滤
    val isR18: Boolean?,                 // R-18 过滤
    val isAI: Boolean?,                  // AI 作品过滤
    val authorIds: List<Long>?,          // 特定作者过滤
    val pathTemplate: String,            // 路径模板
    val isEnabled: Boolean = true        // 是否启用
)
```

**内置默认规则**：
- 插画：`Download/Pixiv/Illustrations/{author_name}_{author_id}/`
- Ugoira：`Download/Pixiv/Ugoira/{author_name}_{author_id}/`
- 漫画：`Download/Pixiv/Manga/{author_name}_{author_id}/`
- 小说：`Download/Pixiv/Novels/{author_name}_{author_id}/`
- R-18：`Download/Pixiv/R18/{resource_type}/`

**3. 文件命名系统**

10 种可用变量：

| 变量 | 说明 | 示例 |
|-----|------|------|
| `{id}` | 作品ID | 123456789 |
| `{p}` | 页码（多页作品） | 001, 002 |
| `{title}` | 作品标题 | 夏日风景 |
| `{author_id}` | 作者ID | 12345 |
| `{author_name}` | 作者名称 | Artist |
| `{publish_date}` | 发布日期 | 20250101 |
| `{download_date}` | 下载日期 | 20251223 |
| `{ai}` | AI标记 | AI 或空 |
| `{r18}` | R-18标记 | R18 或空 |
| `{tags}` | 标签列表 | tag1_tag2 |

**默认模板**：`{id}_{title}_{p}`

**4. 存储权限管理**

Android 全版本适配：
- **Android 7-9**: WRITE_EXTERNAL_STORAGE 权限
- **Android 10+**: MediaStore API + IS_PENDING 原子写入
- **Android 11+**: 分区存储，SAF (Storage Access Framework)

#### 使用示例

```kotlin
// 下载插画
downloadManager.downloadIllust(
    artwork = artwork,
    format = IllustFormat.ORIGINAL
)

// 下载 Ugoira
downloadManager.downloadUgoira(
    artwork = ugoira,
    format = UgoiraFormat.MP4  // ZIP/GIF/MP4
)

// 下载小说
downloadManager.downloadNovel(
    novel = novel,
    format = NovelFormat.EPUB  // TXT/EPUB
)
```

**详细文档**: [docs/guides/下载系统完整设计文档.md](../guides/下载系统完整设计文档.md)

---

## 六、技术栈与依赖版本

> 📋 **完整技术栈文档**: [docs/shared/TECH_STACK.md](../shared/TECH_STACK.md)

### 核心版本概览

| 技术 | 版本 | 说明 |
|-----|------|------|
| Kotlin | 2.2.21 | 编程语言 |
| Compose Multiplatform | 1.9.3 | UI 框架 |
| Android Gradle Plugin | 8.13.2 | Android 构建工具 |
| Koin | 4.1.1 | 依赖注入 |
| Ktor | 3.3.3 | HTTP 客户端 |
| Room | 2.8.4 | 本地数据库 |
| Voyager | 1.1.0-beta03 | 导航框架 |
| Coil | 3.3.0 | 图片加载 |
| DataStore | 1.2.0 | 键值对存储 |
| Okio | 3.16.4 | 文件 I/O |
| GIF.kt | 0.3.0 | GIF 编解码 |
| JavaCV | 1.5.12 | FFmpeg 封装（Desktop） |
| Compose WebView | 2.0.3 | 跨平台 WebView |

### 平台要求

- **Android**: API 24 (Android 7.0) → API 36 (Android 15)
- **编译版本**: Android API 36, Shared API 35
- **JVM**: Java 11
- **Desktop**: Windows 10+ / macOS 10.14+ / Linux

### 主要依赖库

- **架构**: Koin (DI), kotlinx-coroutines (异步)
- **网络**: Ktor (HTTP), kotlinx-serialization (JSON)
- **数据**: Room (SQLite), DataStore (配置), Okio (文件)
- **UI**: Compose (UI 框架), Material 3 (设计系统), Voyager (导航), Coil (图片)

### 版本管理

所有依赖版本在 `gradle/libs.versions.toml` 中统一管理，使用 Gradle Version Catalog 特性。

```toml
[versions]
kotlin = "2.2.21"
koin = "4.1.1"
ktor = "3.3.3"

[libraries]
koin-core = { module = "io.insert-koin:koin-core", version.ref = "koin" }

[plugins]
kotlin-multiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
```

> 📖 详细依赖列表和配置说明请查看: [docs/shared/TECH_STACK.md](../shared/TECH_STACK.md)

---

## 七、项目当前状态

> 🌐 **API 集成状态**: [docs/shared/API_STATUS.md](../shared/API_STATUS.md)

### 功能模块完成度

| 模块 | 状态 | 完成度 | 说明 |
|------|------|--------|------|
| 基础架构 | ✅ 完成 | 100% | Clean Architecture + MVI |
| UI 框架 | ✅ 完成 | 100% | Material 3 + 响应式布局 |
| Pixiv API | ✅ 完成 | 100% | 12 个 API 模块全部完成 |
| 数据持久化 | ✅ 完成 | 100% | Room + DataStore |
| 多语言系统 | ✅ 完成 | 100% | 5 种语言支持 |
| 全局状态缓存 | ✅ 完成 | 100% | 收藏/关注状态跨页面同步 |
| 下载系统 | ✅ 完成 | 90% | 插画/Ugoira/小说下载，路径规则 |
| 登录系统 | ✅ 完成 | 100% | PHPSESSID 配置，自动刷新 |
| 作品浏览 | ✅ 完成 | 100% | 详情页/列表页/瀑布流布局 |
| 用户系统 | ✅ 完成 | 100% | 用户主页/关注/粉丝/作品列表 |
| 排行榜 | ✅ 完成 | 100% | 全部排行榜类型支持 |
| 发现页面 | ✅ 完成 | 100% | 作品/用户推荐 |
| 搜索功能 | ✅ 完成 | 100% | 作品/用户/标签搜索 |
| 评论系统 | ✅ 完成 | 100% | 查看评论/发表评论 |
| 小说功能 | ✅ 完成 | 100% | 小说阅读/系列/收藏 |

### 已完成核心功能 ✅

#### 基础架构
- ✅ Kotlin Multiplatform (Android + Desktop)
- ✅ Clean Architecture 三层分离
- ✅ MVI 架构模式
- ✅ Koin 依赖注入

#### UI 层
- ✅ Material Design 3 主题（深色/浅色模式）
- ✅ 响应式布局系统（手机/平板/桌面适配）
- ✅ 完整页面实现：主页、发现、排行榜、搜索、用户、作品详情、下载管理、设置等
- ✅ UgoiraPlayer 组件（动图播放）
- ✅ WebView 集成（小说阅读）

#### Pixiv API 集成
- ✅ 12 个完整 API 模块 (IllustApi, IllustSeriesApi, UserApi, BookmarkApi, RankingApi, CommentApi, NovelApi, NovelSeriesApi, TagApi, MarkerApi, FollowApi, SearchApi)
- ✅ PHPSESSID 认证
- ✅ CSRF Token 自动管理
- ✅ 完整的 DTO 和 Domain 模型映射

#### 数据持久化
- ✅ Room 数据库（ArtworkEntity, SettingsEntity, DownloadTaskEntity, DownloadRuleEntity）
- ✅ DataStore 配置存储（Pixiv 配置、应用设置）
- ✅ Ugoira 缓存系统（ZIP 下载、解压、播放）
- ✅ 全局状态缓存系统（收藏/关注状态同步）
- ✅ 设置缓存（内存缓存，性能优化）

#### 全局状态缓存系统 ⭐
- ✅ StateCacheManager - 统一状态管理
- ✅ 作品收藏状态（公开/非公开/未收藏）
- ✅ 小说收藏状态
- ✅ 用户关注状态（公开关注/悄悄关注/未关注）
- ✅ 状态变更事件流（响应式更新）
- ✅ 跨页面状态同步
- ✅ UseCase 封装（BookmarkArtworkUseCase 等 11 个）

#### 下载系统 ⭐
- ✅ 插画/漫画下载（单页/多页，原图质量）
- ✅ Ugoira 下载（ZIP/GIF/MP4 三种格式）
- ✅ 小说下载（TXT/EPUB，内嵌图片）
- ✅ 下载路径规则系统（优先级匹配，资源类型/R-18/AI作品/作者过滤）
- ✅ 文件命名规则系统（10种变量，实时预览）
- ✅ 下载任务管理（状态跟踪、进度显示）
- ✅ 存储权限管理（Android 7-16 全版本适配）
- ✅ MediaStore API 集成（Android 10+）

#### 核心功能页面
- ✅ 登录系统（PHPSESSID 配置）
- ✅ 作品详情页（大图预览、收藏、下载、评论）
- ✅ 用户主页（作品列表、关注/粉丝、好P友）
- ✅ 发现页面（推荐作品、推荐用户）
- ✅ 排行榜（全部模式支持）
- ✅ 搜索功能（作品、用户、标签）
- ✅ 漫画系列页面（章节列表、追更）
- ✅ 小说阅读（WebView 渲染）
- ✅ 关注动态（已关注用户的最新作品）
- ✅ 评论管理（查看、发表、删除）

#### 多语言
- ✅ 5 种语言（简中/繁中/英文/日文/韩文）
- ✅ LocaleManager 动态切换
- ✅ Compose Resources 官方方案
- ✅ 应用语言与 API 语言分离管理

### 下一阶段重点 🎯

#### 优化计划
- 性能优化：列表滚动性能提升
- 缓存策略：图片缓存管理优化
- 内存管理：大图加载内存优化
- 网络优化：请求合并和取消策略

---

## 八、开发指南

### 8.1 添加新页面

**步骤**:
1. 在 `composeApp/ui/screens/[feature]/` 创建目录
2. 定义 MVI 组件（State、Intent、ViewModel）
3. 创建 Screen 类（继承 `Screen`）
4. 在导航中注册

**示例**:
```kotlin
// 1. State & Intent
data class ArtworkDetailState(
    val artwork: Artwork? = null,
    val isLoading: Boolean = false,
    val error: String? = null
)

sealed interface ArtworkDetailIntent {
    data class LoadArtwork(val id: String) : ArtworkDetailIntent
    data object ToggleBookmark : ArtworkDetailIntent
}

// 2. ViewModel
class ArtworkDetailViewModel(
    private val artworkRepository: ArtworkRepository
) : ViewModel() {
    private val _state = MutableStateFlow(ArtworkDetailState())
    val state = _state.asStateFlow()
    
    fun handleIntent(intent: ArtworkDetailIntent) { ... }
}

// 3. Screen
class ArtworkDetailScreen(private val artworkId: String) : Screen {
    @Composable
    override fun Content() {
        val viewModel = koinViewModel<ArtworkDetailViewModel>()
        val state by viewModel.state.collectAsState()
        
        // UI 实现...
    }
}
```

### 8.2 添加新 API

**步骤**:
1. 在 `shared/data/remote/dto/pixiv/` 定义 DTO
2. 在 `shared/data/remote/mapper/` 创建 Mapper
3. 在 `shared/data/remote/api/` 添加 API 方法
4. 在 `shared/domain/model/` 定义 Domain Model
5. 在 `shared/domain/repository/` 定义 Repository 接口
6. 在 `shared/data/repository/` 实现 Repository

**示例**:
```kotlin
// 1. DTO
@Serializable
data class CommentDto(
    val id: String,
    val comment: String,
    val user: UserDto
)

// 2. Mapper
fun CommentDto.toDomain() = Comment(
    id = id,
    content = comment,
    user = user.toDomain()
)

// 3. API
class IllustApi(private val client: PixivApiClient) {
    suspend fun getComments(illustId: String): List<CommentDto> {
        return client.get("/ajax/illusts/comments/roots") {
            parameter("illust_id", illustId)
        }
    }
}

// 4. Domain Model
data class Comment(
    val id: String,
    val content: String,
    val user: User
)

// 5. Repository Interface
interface CommentRepository {
    suspend fun getComments(illustId: String): Result<List<Comment>>
}

// 6. Repository Implementation
class CommentRepositoryImpl(
    private val pixivApi: PixivApi
) : CommentRepository {
    override suspend fun getComments(illustId: String): Result<List<Comment>> {
        return try {
            val comments = pixivApi.illustApi.getComments(illustId)
            Result.success(comments.map { it.toDomain() })
        } catch (e: Exception) {
            Result.failure(e)
        }
    }
}
```

### 8.3 添加多语言字符串

**步骤**:
1. 在所有语言的 `strings.xml` 中添加相同的 key
2. 在代码中使用 `stringResource(Res.string.your_key)`

**示例**:
```xml
<!-- values/strings.xml (英文) -->
<string name="artwork_detail_title">Artwork Details</string>

<!-- values-zh-rCN/strings.xml (简体中文) -->
<string name="artwork_detail_title">作品详情</string>

<!-- values-ja/strings.xml (日文) -->
<string name="artwork_detail_title">作品詳細</string>
```

```kotlin
// 在 Composable 中使用
Text(stringResource(Res.string.artwork_detail_title))
```

### 8.4 使用 Pixiv API

**获取 PHPSESSID**:
1. 在浏览器中登录 Pixiv
2. 打开开发者工具 → Application/Storage → Cookies
3. 复制 `PHPSESSID` 的值

**初始化 API 客户端**:
```kotlin
// 在 Koin 模块中
single {
    val config = get<PixivConfigStore>().config.first()
    PixivApi.create(
        httpClient = get(),
        phpSessionId = config.phpSessionId,
        token = config.csrfToken,
        lang = config.language
    )
}
```

**调用 API**:
```kotlin
// 在 Repository 中
class ArtworkRepositoryImpl(
    private val pixivApi: PixivApi
) : ArtworkRepository {
    override suspend fun getArtworkDetail(id: String): Result<Artwork> {
        return try {
            val dto = pixivApi.illustApi.getIllustDetail(id)
            Result.success(dto.toDomain())
        } catch (e: Exception) {
            Result.failure(e)
        }
    }
}
```

**参考文档**: `docs/guides/API测试工具使用指南.md`

### 8.5 注意事项

#### 平台差异处理
使用 `expect`/`actual` 关键字：

```kotlin
// commonMain
expect fun calculateWindowSizeClass(): WindowSizeClass

// androidMain
actual fun calculateWindowSizeClass(): WindowSizeClass {
    // Android 实现
}

// desktopMain
actual fun calculateWindowSizeClass(): WindowSizeClass {
    // Desktop 实现
}
```

#### 协程作用域
- UI 层: `viewModelScope.launch { }`
- Repository 层: `withContext(Dispatchers.IO) { }`
- 数据库操作: Room 自动处理，无需手动切换

#### 错误处理
统一使用 `Result<T>`:

```kotlin
suspend fun fetchData(): Result<Data> {
    return try {
        val data = api.getData()
        Result.success(data)
    } catch (e: Exception) {
        Result.failure(e)
    }
}

// 使用时
result.onSuccess { data ->
    // 处理成功
}.onFailure { error ->
    // 处理错误
}
```

#### 资源释放
- 图片加载使用 Coil，自动管理内存
- 文件流需要使用 `use { }` 自动关闭
- Room 数据库连接由框架管理

---

## 九、相关文档

| 文档 | 路径 | 说明 |
|-----|------|------|
| **README** | `README.md` | 项目概览和快速开始 |
| **API 测试工具使用指南** | `docs/guides/API测试工具使用指南.md` | API 使用和代码集成指南 |
| **设置系统架构** | `docs/settings/设置系统架构.md` | 设置功能设计 |
| **自适应布局指南** | `docs/guides/自适应布局指南.md` | 响应式布局实现 |

---

## 十、构建和运行

### Android
```bash
# 构建 APK
./gradlew :composeApp:assembleDebug

# 安装到设备
./gradlew :composeApp:installDebug

# 或使用脚本
./build-android.sh  # Linux/Mac
build-android.bat   # Windows
```

### Desktop
```bash
# 运行
./gradlew :composeApp:run

# 打包
./gradlew :composeApp:createDistributable

# 或使用脚本
./build-desktop.sh  # Linux/Mac
build-desktop.bat   # Windows
```

### 清理构建
```bash
./gradlew clean
```

---

> 📅 **文档版本**: v2.0  
> 📝 **最后更新**: 2025-12-23  
> 👤 **维护者**: ProjectU Team  
> 
> 如有问题或建议，请查看 README.md 或提交 Issue。
