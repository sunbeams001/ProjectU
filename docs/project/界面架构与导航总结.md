# ProjectU - 界面架构与导航总结

> 📅 最后更新: 2025-11-26  
> 🎯 目标: 快速查找项目主要界面、组件和跳转逻辑  
> 🏗️ 架构: Clean Architecture + MVI + 响应式布局

---

## 📱 主要界面概览

### 界面分类

| 类别 | 界面名称 | 路径 | 状态 | 说明 |
|-----|---------|------|------|------|
| **认证** | LoginScreen | `ui/screens/login/` | ✅ 已完成 | PHPSESSID 登录 |
| **主框架** | HomeScreen | `ui/screens/home/` | ✅ 已完成 | 底部/侧边导航容器 |
| **发现** | DiscoveryIllustsScreen | `ui/screens/discovery/` | ✅ 已完成 | 推荐插画·漫画 |
| **发现** | DiscoveryNovelsScreen | `ui/screens/discovery/` | ✅ 已完成 | 推荐小说 |
| **发现** | DiscoveryUsersScreen | `ui/screens/discovery/` | ✅ 已完成 | 推荐用户 |
| **设置** | SettingsScreen | `ui/screens/settings/` | ✅ 已完成 | 应用设置 |
| **工具** | ApiTestScreen | `ui/screens/apitest/` | ✅ 已完成 | API 测试工具 |

---

## 🗺️ 导航架构

### 导航层级结构

```
App (Navigator)
├── LoginScreen (未登录时)
│   └── 登录成功 → replaceAll(HomeScreen)
│
└── HomeScreen (已登录时，TabNavigator)
    ├── HomeTab (主页)
    ├── DiscoveryTab (发现)
    │   ├── → DiscoveryUsersScreen (推荐用户)
    │   ├── → DiscoveryIllustsScreen (推荐插画)
    │   └── → DiscoveryNovelsScreen (推荐小说)
    ├── RankingTab (排行榜，占位)
    └── ProfileTab (个人)
        └── → SettingsScreen (设置)
            └── → ApiTestScreen (API测试)
```

### 导航实现方式

**使用 Voyager 导航框架**：
- **Navigator**: 全局页面栈导航
- **TabNavigator**: Tab 页签导航（主页底部导航）
- **Screen**: 所有页面继承自 `Screen` 接口

**导航操作**：
```kotlin
// 跳转到新页面
navigator.push(SettingsScreen())

// 替换当前页面
navigator.replace(HomeScreen())

// 替换所有页面（登录成功）
navigator.replaceAll(HomeScreen())

// 返回上一页
navigator.pop()

// Tab 切换
tabNavigator.current = DiscoveryTab
```

---

## 📄 详细界面说明

### 1. LoginScreen - 登录界面

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/login/LoginScreen.kt`

**功能**:
- PHPSESSID 输入登录（当前实现）
- 应用内登录（预留，未实现）
- 帮助对话框（如何获取 PHPSESSID）

**MVI 组件**:
- **State**: `LoginScreenState` - 登录状态、错误信息、加载中
- **Intent**: `LoginIntent` - 用户操作（输入变化、登录、切换模式）
- **ViewModel**: `LoginViewModel` - 状态管理和业务逻辑

**导航逻辑**:
```kotlin
// 登录成功后
navigator.replaceAll(HomeScreen())  // 替换整个导航栈
```

**布局**:
- 单页面，垂直滚动布局
- 顶部 AppBar + 帮助按钮
- 中央登录表单（Card 样式）
- 密码可见性切换
- 加载状态指示器

---

### 2. HomeScreen - 主界面框架

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/home/HomeScreen.kt`

**功能**:
- 应用主框架，包含底部/侧边导航
- 响应式布局：手机用底部导航，平板/桌面用侧边导航
- 4 个主要 Tab 页签

**响应式布局**:
```kotlin
// 手机布局
HomeScreenPhone(windowSize)  
  └── Scaffold + NavigationBar (底部)

// 平板/桌面布局
HomeScreenTablet(windowSize)  
  └── Row + NavigationRail (左侧)
```

**Tab 页签**:

| Tab | Icon | 索引 | 内容 |
|-----|------|------|------|
| HomeTab | Home | 0 | 主页（占位） |
| DiscoveryTab | Search | 1 | 发现（跳转按钮） |
| RankingTab | Star | 2 | 排行榜（占位） |
| ProfileTab | Person | 3 | 个人（设置按钮） |

**导航逻辑**:
```kotlin
// 从 Tab 跳转到全屏页面（需要获取父级 Navigator）
val parentNavigator = LocalNavigator.current?.parent
parentNavigator?.push(DiscoveryIllustsScreen())
```

---

### 3. DiscoveryIllustsScreen - 推荐插画页面

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/discovery/DiscoveryIllustsScreen.kt`

**功能**:
- 展示 Pixiv 推荐插画和漫画
- 瀑布流布局（StaggeredGrid）
- 模式切换：ALL（全部）、SAFE（安全）、R18
- 无限滚动加载

**MVI 组件**:
- **State**: `DiscoveryIllustsState` - 作品列表、加载状态、当前模式
- **ViewModel**: `DiscoveryIllustsViewModel` - 数据加载和状态管理

**UI 结构**:
```
Scaffold
├── TopAppBar (标题 + 返回按钮)
├── ModeSelector (ALL / SAFE / R18)
└── LazyVerticalStaggeredGrid (瀑布流)
    └── ArtworkCard (作品卡片)
```

**导航**:
```kotlin
// 返回上一页
navigator.pop()

// 点击作品（TODO）
// navigator.push(ArtworkDetailScreen(artworkId))
```

**API 调用**:
- `PixivApi.illustApi.getDiscoveryArtworks(mode)`
- 支持分页加载（limit + lastId）

---

### 4. DiscoveryNovelsScreen - 推荐小说页面

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/discovery/DiscoveryNovelsScreen.kt`

**功能**:
- 展示 Pixiv 推荐小说
- 瀑布流布局
- 模式切换：ALL、SAFE、R18
- 无限滚动加载

**MVI 组件**:
- **State**: `DiscoveryNovelsState` - 小说列表、加载状态、当前模式
- **ViewModel**: `DiscoveryNovelsViewModel`

**UI 结构**:
```
Scaffold
├── TopAppBar
├── ModeSelector
└── LazyVerticalStaggeredGrid
    └── NovelCard (小说卡片)
```

**API 调用**:
- `PixivApi.novelApi.getDiscoveryNovels(mode)`

---

### 5. DiscoveryUsersScreen - 推荐用户页面

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/discovery/DiscoveryUsersScreen.kt`

**功能**:
- 展示 Pixiv 推荐用户
- 网格布局
- 关注/取关操作
- 点击跳转用户主页（TODO）

**MVI 组件**:
- **State**: `DiscoveryUsersState` - 用户列表、加载状态
- **ViewModel**: `DiscoveryUsersViewModel`

**UI 结构**:
```
Scaffold
├── TopAppBar
└── LazyVerticalGrid (3列)
    └── UserCard (用户卡片)
```

**API 调用**:
- `PixivApi.userApi.getDiscoveryUsers()`
- `PixivApi.userApi.followUser(userId)` / `unfollowUser(userId)`

---

### 6. SettingsScreen - 设置界面

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/settings/SettingsScreen.kt`

**功能**:
- 应用语言设置（简中/繁中/英文/日文/韩文）
- Pixiv API 语言设置
- 主题模式（浅色/深色/跟随系统）
- Pixiv 配置（PHPSESSID）
- 退出登录
- API 测试工具入口

**MVI 组件**:
- **State**: `SettingsScreenState` - 所有设置项的当前值
- **Intent**: `SettingsIntent` - 设置项变更操作
- **ViewModel**: `SettingsViewModel`

**UI 结构**:
```
Scaffold
├── TopAppBar
└── Column (Scrollable)
    ├── 应用设置 Section
    │   ├── 应用语言
    │   ├── Pixiv 语言
    │   └── 主题模式
    ├── Pixiv 配置 Section
    │   └── PHPSESSID
    ├── 开发者工具 Section
    │   └── API 测试工具
    └── 账户 Section
        └── 退出登录
```

**数据持久化**:
- 应用设置 → Room 数据库 (`SettingsEntity`)
- Pixiv 配置 → DataStore (`PixivConfigStore`)

**导航**:
```kotlin
// 打开 API 测试工具
navigator.push(ApiTestScreen())

// 退出登录后
navigator.replaceAll(LoginScreen())
```

---

### 7. ApiTestScreen - API 测试工具

**路径**: `composeApp/src/commonMain/kotlin/com/projectu/ui/screens/apitest/ApiTestScreen.kt`

**功能**:
- 测试所有 Pixiv API 接口
- 选择 API 模块和方法
- 输入参数
- 查看结果（摘要 + 完整 JSON）
- 开发调试工具

**MVI 组件**:
- **State**: `ApiTestScreenState` - 测试状态、结果
- **Intent**: `ApiTestIntent` - 用户操作
- **ViewModel**: `ApiTestViewModel`

**支持的 API 模块**:
- IllustApi (6 个方法)
- UserApi (10 个方法)
- BookmarkApi (8 个方法)
- RankingApi (2 个方法)
- CommentApi (8 个方法)
- NovelApi (5 个方法)
- NovelSeriesApi (5 个方法)
- TagApi (4 个方法)
- MarkerApi (3 个方法)

**UI 结构**:
```
Scaffold
├── TopAppBar
└── Column
    ├── API 模块选择 (Dropdown)
    ├── API 方法选择 (Dropdown)
    ├── 参数输入区域 (动态生成)
    ├── 执行按钮
    └── 结果展示区域
        ├── 结果摘要 (Card)
        └── 完整 JSON (可展开)
```

---

## 🧩 主要可复用组件

### UI 组件

| 组件名 | 路径 | 功能 | 使用场景 |
|-------|------|------|---------|
| **AdaptiveLayout** | `ui/components/AdaptiveLayout.kt` | 响应式布局容器 | 所有需要适配的页面 |
| **ArtworkCard** | `ui/components/ArtworkCard.kt` | 作品卡片 | 推荐插画、搜索结果 |
| **NovelCard** | `ui/components/NovelCard.kt` | 小说卡片 | 推荐小说、搜索结果 |
| **UserCard** | `ui/components/UserCard.kt` | 用户卡片 | 推荐用户、关注列表 |
| **BookmarkIndicator** | `ui/components/BookmarkIndicator.kt` | 收藏状态指示器 | 作品卡片、详情页 |
| **NovelBookmarkIndicator** | `ui/components/NovelBookmarkIndicator.kt` | 小说收藏指示器 | 小说卡片、详情页 |
| **FollowIndicator** | `ui/components/FollowIndicator.kt` | 关注状态指示器 | 用户卡片、用户详情 |
| **ArtworkBadges** | `ui/components/ArtworkBadges.kt` | 作品标签徽章 | 作品卡片（R18、AI 等） |
| **UgoiraPlayer** | `ui/components/UgoiraPlayer.kt` | Ugoira 动图播放器 | 作品详情页 |

### 响应式布局工具

**位置**: `ui/util/WindowSizeClass.kt`

```kotlin
// 窗口尺寸分类
enum class WindowSizeClass {
    COMPACT,    // < 600dp (手机)
    MEDIUM,     // 600-840dp (平板)
    EXPANDED    // > 840dp (桌面)
}

// 获取窗口尺寸
@Composable
fun rememberWindowSize(): WindowSize

// 自适应布局容器
@Composable
fun SimpleAdaptiveLayout(
    phoneContent: @Composable (WindowSize) -> Unit,
    tabletContent: @Composable (WindowSize) -> Unit
)

@Composable
fun AdaptiveLayout(
    phoneContent: @Composable (WindowSize) -> Unit,
    tabletContent: @Composable (WindowSize) -> Unit,
    desktopContent: @Composable (WindowSize) -> Unit
)
```

### 主题组件

**位置**: `ui/theme/`

```kotlin
// 主题模式
enum class ThemeMode {
    LIGHT,      // 浅色
    DARK,       // 深色
    SYSTEM      // 跟随系统
}

// 应用主题
@Composable
fun AppTheme(
    themeMode: ThemeMode = ThemeMode.SYSTEM,
    content: @Composable () -> Unit
)
```

**Material 3 配色**:
- Primary: Pixiv 蓝色系
- 支持深色/浅色模式自动切换
- 完整的 ColorScheme 定义

---

## 🎭 MVI 架构模式

### 标准 MVI 实现

所有页面都遵循统一的 MVI 架构模式：

```kotlin
// 1. State - 页面状态
data class XxxScreenState(
    val data: List<Item> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
)

// 2. Intent - 用户操作
sealed interface XxxIntent {
    data object LoadData : XxxIntent
    data class OnItemClick(val id: String) : XxxIntent
}

// 3. ViewModel - 状态管理
class XxxViewModel(
    private val repository: Repository
) : ScreenModel {
    private val _state = MutableStateFlow(XxxScreenState())
    val state: StateFlow<XxxScreenState> = _state.asStateFlow()
    
    init {
        loadData()
    }
    
    fun handleIntent(intent: XxxIntent) {
        when (intent) {
            is XxxIntent.LoadData -> loadData()
            is XxxIntent.OnItemClick -> handleClick(intent.id)
        }
    }
    
    private fun loadData() {
        screenModelScope.launch {
            _state.update { it.copy(isLoading = true) }
            repository.getData()
                .onSuccess { data -> 
                    _state.update { it.copy(data = data, isLoading = false) }
                }
                .onFailure { error ->
                    _state.update { it.copy(error = error.message, isLoading = false) }
                }
        }
    }
}

// 4. Screen - UI 实现
class XxxScreen : Screen {
    @Composable
    override fun Content() {
        val viewModel = koinScreenModel<XxxViewModel>()
        val state by viewModel.state.collectAsState()
        
        XxxScreenContent(
            state = state,
            onIntent = viewModel::handleIntent
        )
    }
}

@Composable
private fun XxxScreenContent(
    state: XxxScreenState,
    onIntent: (XxxIntent) -> Unit
) {
    // UI 实现
}
```

### 依赖注入（Koin）

```kotlin
// 在 AppModule.kt 中注册
val viewModelModule = module {
    // ScreenModel (Voyager 的 ViewModel)
    factoryOf(::DiscoveryIllustsViewModel)
    factoryOf(::SettingsViewModel)
    // ...
}
```

---

## 📱 响应式布局规范

### 布局断点

| 设备类型 | 宽度范围 | WindowSizeClass | 导航样式 | 网格列数 |
|---------|---------|-----------------|---------|---------|
| 手机竖屏 | < 600dp | COMPACT | 底部导航栏 | 2 列 |
| 手机横屏/小平板 | 600-840dp | MEDIUM | 侧边导航栏 | 3 列 |
| 大平板/桌面 | > 840dp | EXPANDED | 侧边导航栏 | 4 列 |

### 使用示例

```kotlin
@Composable
fun MyScreen() {
    SimpleAdaptiveLayout(
        phoneContent = { windowSize ->
            // 手机布局
            Scaffold(
                bottomBar = { NavigationBar { /* tabs */ } }
            ) {
                LazyVerticalGrid(columns = GridCells.Fixed(2)) {
                    // 2列网格
                }
            }
        },
        tabletContent = { windowSize ->
            // 平板/桌面布局
            Row {
                NavigationRail { /* tabs */ }
                LazyVerticalGrid(columns = GridCells.Fixed(4)) {
                    // 4列网格
                }
            }
        }
    )
}
```

---

## 🔄 全局状态管理

### 状态缓存系统

**位置**: `shared/domain/cache/`

**功能**:
- 作品收藏状态全局同步
- 小说收藏状态全局同步
- 用户关注状态全局同步
- 跨页面状态响应式更新

**使用示例**:
```kotlin
// 在 ViewModel 中
class MyViewModel(
    private val stateCacheManager: StateCacheManager
) : ScreenModel {
    val bookmarkStates = stateCacheManager.illustBookmarkStates
    
    fun bookmarkArtwork(artworkId: String) {
        screenModelScope.launch {
            bookmarkArtworkUseCase(artworkId, BookmarkRestrict.PUBLIC)
        }
    }
}

// 在 Composable 中
@Composable
fun ArtworkCard(artwork: Artwork) {
    val bookmarkStates by stateCacheManager
        .illustBookmarkStates
        .collectAsState()
    
    val isBookmarked = bookmarkStates[artwork.id]?.isBookmarked ?: false
    
    BookmarkIndicator(
        isBookmarked = isBookmarked,
        onClick = { /* ... */ }
    )
}
```

详细文档: [全局状态缓存使用指南](../guides/全局状态缓存使用指南.md)

---

## 🌍 多语言系统

### 语言管理

**位置**: `ui/localization/LocaleManager.kt`

**支持的语言**:
- 简体中文 (zh-CN)
- 繁体中文 (zh-TW)
- English (en)
- 日本語 (ja)
- 한국어 (ko)

**使用方式**:
```kotlin
// 获取字符串资源
Text(stringResource(Res.string.nav_home))

// 切换语言
localeManager.setLanguage(AppLanguage.SIMPLIFIED_CHINESE)

// 观察语言变化
val currentLanguage by localeManager.currentLanguage.collectAsState()
```

**资源文件位置**:
```
composeApp/src/commonMain/composeResources/
├── values/              # 英文（默认）
├── values-zh-rCN/      # 简体中文
├── values-zh-rTW/      # 繁体中文
├── values-ja/          # 日文
└── values-ko/          # 韩文
```

---

## 🎨 图片加载

### Coil 3 图片加载

**配置位置**: `ui/util/ImageLoaderFactory.kt`

**自动处理**:
- Pixiv Referer 头（必需）
- 内存缓存
- 磁盘缓存
- 网络图片懒加载

**使用方式**:
```kotlin
AsyncImage(
    model = imageUrl,
    contentDescription = null,
    modifier = Modifier.size(200.dp)
)
```

**Pixiv 图片 URL 处理**:
```kotlin
// Pixiv 需要特定的 Referer
httpClient.config {
    defaultRequest {
        header("Referer", "https://www.pixiv.net/")
    }
}
```

---

## 🔍 快速查找指南

### 我想找...

| 需求 | 查找位置 |
|-----|---------|
| **添加新页面** | 参考 `DiscoveryIllustsScreen` 实现 MVI 模式 |
| **修改导航** | 查看 `HomeScreen.kt` 的 Tab 和导航逻辑 |
| **添加新 API** | 查看 `docs/pixiv/PIXIV_API_集成指南.md` |
| **修改主题** | 查看 `ui/theme/Theme.kt` |
| **添加多语言** | 编辑 `composeResources/values-*/strings.xml` |
| **调整布局断点** | 修改 `ui/util/WindowSizeClass.kt` |
| **创建可复用组件** | 放在 `ui/components/` 目录 |
| **修改设置项** | 编辑 `SettingsScreen.kt` 和 `SettingsViewModel.kt` |
| **测试 API** | 使用 `ApiTestScreen` |
| **理解状态管理** | 阅读 `docs/guides/全局状态缓存使用指南.md` |

### 常见任务示例

#### 1. 添加新的 Tab 页签

```kotlin
// 在 HomeScreen.kt 中

// 1. 定义 Tab 对象
object NewTab : Tab {
    override val options: TabOptions
        @Composable
        get() = TabOptions(
            index = 4u,
            title = "新页签",
            icon = rememberVectorPainter(Icons.Default.NewIcon)
        )
    
    @Composable
    override fun Content() {
        // Tab 内容
    }
}

// 2. 在导航栏中添加
NavigationBarItem(
    selected = tabNavigator.current == NewTab,
    onClick = { tabNavigator.current = NewTab },
    icon = { Icon(Icons.Default.NewIcon, null) },
    label = { Text("新页签") }
)
```

#### 2. 创建新的全屏页面

```kotlin
// 1. 创建 State
data class MyScreenState(
    val data: List<Item> = emptyList(),
    val isLoading: Boolean = false
)

// 2. 创建 Intent
sealed interface MyIntent {
    data object Load : MyIntent
}

// 3. 创建 ViewModel
class MyViewModel(
    private val repository: MyRepository
) : ScreenModel {
    private val _state = MutableStateFlow(MyScreenState())
    val state = _state.asStateFlow()
    
    init { loadData() }
    
    fun handleIntent(intent: MyIntent) { /* ... */ }
}

// 4. 创建 Screen
class MyScreen : Screen {
    @Composable
    override fun Content() {
        val viewModel = koinScreenModel<MyViewModel>()
        val state by viewModel.state.collectAsState()
        
        MyScreenContent(state, viewModel::handleIntent)
    }
}

// 5. 注册 ViewModel（在 AppModule.kt）
factoryOf(::MyViewModel)

// 6. 跳转
navigator.push(MyScreen())
```

#### 3. 添加响应式布局

```kotlin
@Composable
fun MyScreen() {
    SimpleAdaptiveLayout(
        phoneContent = { windowSize ->
            // 手机布局：单列，底部操作
            Column {
                Content()
                BottomBar()
            }
        },
        tabletContent = { windowSize ->
            // 平板布局：双列，侧边操作
            Row {
                SidePanel()
                Content()
            }
        }
    )
}
```

---

## 📊 开发状态

| 模块 | 完成度 | 说明 |
|-----|--------|------|
| 登录系统 | ✅ 80% | PHPSESSID 登录完成，应用内登录待实现 |
| 主框架 | ✅ 100% | 响应式导航完成 |
| 发现功能 | ✅ 100% | 推荐插画/小说/用户完成 |
| 设置系统 | ✅ 100% | 所有设置项完成 |
| 作品详情 | ⏳ 0% | 待实现 |
| 用户主页 | ⏳ 0% | 待实现 |
| 搜索功能 | ⏳ 0% | 待实现 |
| 排行榜 | ⏳ 0% | 待实现 |

---

## 🔗 相关文档

| 文档 | 说明 |
|-----|------|
| [项目架构参考文档](项目架构参考文档.md) | Clean Architecture 详细说明 |
| [自适应布局指南](../guides/自适应布局指南.md) | 响应式布局最佳实践 |
| [全局状态缓存使用指南](../guides/全局状态缓存使用指南.md) | 状态管理详解 |
| [Pixiv API 集成指南](../pixiv/PIXIV_API_集成指南.md) | API 使用文档 |
| [API 状态](../shared/API_STATUS.md) | API 集成进度 |
| [开发进度](../shared/DEVELOPMENT_STATUS.md) | 整体开发状态 |

---

> 📝 **提示**: 本文档会随着功能开发持续更新。开发新功能前，请先参考本文档了解现有架构和规范。

> 🎯 **快速开始**: 
> 1. 查看 `LoginScreen` 了解 MVI 模式
> 2. 查看 `DiscoveryIllustsScreen` 了解列表页面
> 3. 查看 `HomeScreen` 了解响应式布局
> 4. 查看 `ArtworkCard` 了解可复用组件

---

**最后更新**: 2025-11-26  
**文档版本**: v1.0  
**维护者**: ProjectU Team

